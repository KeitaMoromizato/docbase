{
  "name": "docbase",
  "repository": {
    "type": "git",
    "url": "https://github.com/KeitaMoromizato/docbase.git"
  },
  "version": "1.0.2",
  "description": "This module provides an easy way to use DocBase API.",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build",
    "test": "mocha test/**/*.js"
  },
  "files": [
    "lib",
    "src"
  ],
  "keywords": [
    "DocBase"
  ],
  "author": "Keita Moromizato",
  "email": "keita.moromi@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "espower-babel": "^4.0.3",
    "mocha": "^2.5.3",
    "power-assert": "^1.4.1"
  },
  "dependencies": {
    "request": "^2.67.0"
  }
}
